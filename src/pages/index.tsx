import type { NextPage } from "next";
import { Button } from "@nextui-org/react";

const Home: NextPage = () => {
  return (
    <div>
      Hello World!
      <Button>Testing</Button>
    </div>
  );
};

export default Home;
